package cs3500.model.transformation;

/**
 * Represents the different transformation types. The transformation types are color change,
 * size change (scaling), and position change (moving).
 */
public enum TransformType {
  COLOR, SCALE, POSITION
}
